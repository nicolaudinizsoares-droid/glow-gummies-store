// Where Glow is, off-site.
//
// One place, so a footer button, a share link and any structured data all
// point at the same account rather than drifting apart. Add a key here when an
// account exists; the footer renders a button for each one it finds, and an
// account with no button is better than a button with no account.

export const SOCIAL = {
  facebook: "https://www.facebook.com/profile.php?id=61594047304680",
} as const;
